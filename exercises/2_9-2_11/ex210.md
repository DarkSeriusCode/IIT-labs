# Упражнение 2.10

Наше приложение использует следующий `docker-compose.yml` файл:
```yaml
services:
  frontend:
    image: rustykrabik/frontend
    build:
      context: example-frontend
      args:
        REACT_APP_BACKEND_URL: http://localhost/api

  backend:
    image: rustykrabik/backend
    build: example-backend
    environment:
      - REQUEST_ORIGIN=http://localhost/
      - REDIS_HOST=redis
      - POSTGRES_HOST=db
      - POSTGRES_USER=God
      - POSTGRES_PASSWORD=Jesus
    depends_on:
      - redis
      - db

  redis:
    image: redis:8

  db:
    image: postgres:13.2-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=God
      - POSTGRES_PASSWORD=Jesus
    volumes:
      - database:/var/lib/postgresql/data

  proxy:
    image: nginx
    restart: unless-stopped
    ports:
      - 80:80
    depends_on:
      - frontend
      - backend
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf


volumes:
  database:
```

Давайте найдём открытые порты и избавимся от них. Запустим сканер портов:
```
docker run -it --rm --network host networkstatic/nmap localhost
```

Вывод:
```
Starting Nmap 7.92 ( https://nmap.org ) at 2026-03-23 16:52 UTC
Nmap scan report for localhost (127.0.0.1)
Host is up (0.0000090s latency).
Other addresses for localhost (not scanned): ::1
rDNS record for 127.0.0.1: localhost.localdomain
Not shown: 999 closed tcp ports (reset)
PORT   STATE SERVICE
80/tcp open  http

Nmap done: 1 IP address (1 host up) scanned in 0.11 seconds
```

Как видно, открытых портов нет, значит мы всё сделали правильно и не нужно ничего править
